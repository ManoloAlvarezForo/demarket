{
  "name": "demarket-monorepo",
  "private": true,
  "packageManager": "npm@11.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "rm -rf node_modules && rm -rf apps/*/node_modules"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
